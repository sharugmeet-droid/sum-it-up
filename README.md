# 📄 main.dart – Sum It Up App

This file is the **entry point** of the **Sum It Up** Flutter application.  
It contains the complete UI and logic required to take user input, calculate the sum of numbers, and display the result in a custom message format.

---

## 🚀 Purpose of `main.dart`

- Launches the Flutter application
- Builds the main user interface
- Accepts comma-separated numbers from the user
- Calculates and displays the sum dynamically

---

## 🧠 Key Components

### 1️⃣ `main()`
```dart
void main() {
  runApp(const SumItUpApp());
}
